[DEFAULT]
run_time = 30
rampup = 0
output_dir = /tmp
test_name = 'notes'
endpoint = "http://localhost:8081/dataserver2'

[insertion-100]
runners = 1
target = nti.integrationtests.performance.eval.notes.create_notes
target_args = (100,0)

