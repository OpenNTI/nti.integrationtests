[DEFAULT]
rampup = 0
runners = 8
run_time = 30
test_name = sample
output_dir = /tmp

# serialize group execution
serialize = True

# use threads when calling group target
use_threads = True

min_words = 10
max_words = 40 

# script setup 
script_setup = nti.mechanize.tests.sample.script_setup

# script teardown
script_teardown = nti.mechanize.tests.sample.script_teardown

[creation]
runners = 1
max_iterations = 50
target = nti.mechanize.tests.sample.creation
target_args = ("foo",1)
setup = nti.mechanize.tests.sample.setup
teardown = nti.mechanize.tests.sample.teardown
